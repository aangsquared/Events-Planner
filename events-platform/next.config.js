/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      's1.ticketm.net',
      's2.ticketm.net',
      's3.ticketm.net',
      'images.universe.com'
    ],
  },
}

module.exports = nextConfig 